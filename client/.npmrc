# @chatscope/chat-ui-kit-react declara peer react <=18, mas roda em React 19
# (lib presentational). legacy-peer-deps deixa o `npm install` (inclusive o do
# container web no docker-compose) resolver sem travar no check de peer.
legacy-peer-deps=true
